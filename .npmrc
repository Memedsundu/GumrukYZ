# Hoist all dependencies to the root node_modules.
# Required for Vercel serverless deployment — pnpm's default symlink structure
# causes "symlinked directories" errors in the serverless function bundle.
shamefully-hoist=true
